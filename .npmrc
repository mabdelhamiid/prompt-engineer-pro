# pnpm configuration file

# Use the pnpmfile.cjs for hooks
# shamefully-hoist=true

# Package patching configuration
# Patches are defined in patches/ directory and referenced in pnpmfile.cjs

# Allow peer dependency warnings
strict-peer-dependencies=false

# Use lockfile for reproducible installs
lockfile=true
